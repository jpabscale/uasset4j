# Gradle wrapper bootstrap. The wrapper jar is NOT committed; gradlew / gradlew.bat
# fetch it from the pinned Gradle release tag and verify this SHA-256. Keep the
# version in sync with gradle/wrapper/gradle-wrapper.properties.
gradle.wrapper.version=9.6.1
gradle.wrapper.sha256=497c8c2a7e5031f6aa847f88104aa80a93532ec32ee17bdb8d1d2f67a194a9c7

# Pinned upstream UAssetAPI commit the port tracks (see README). CI uses this to
# fetch the UAssetAPI.Tests/TestAssets corpus, which is not committed here.
uassetapi.pinned.sha=33ef77e5a309062ea80b4a939f34ae8579c2d3bb

# Pinned upstream CUE4Parse commit the curve derivative (EXC-002) tracks. The
# curves port was authored against this exact tree; nothing changed upstream since,
# so the pin is informational today but becomes the single source of truth for the
# parity audit (tools/cue4parse_parity.py) once the derivative is extended.
cue4parse.pinned.sha=e9f24e03c33d60a00dfd7e4d012bad26c6509547

